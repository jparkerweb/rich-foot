/**
 * Centralized constants for Rich Foot plugin
 * Timing values and CSS variable names
 */

// Timing constants (in milliseconds)
export const TIMING = {
    OBSERVER_DEBOUNCE_MS: 150,
    FOOTER_RECHECK_DELAY_MS: 100,
    DEFAULT_UPDATE_DELAY_MS: 3000
};

// CSS variable names for theme colors
export const CSS_VARS = {
    TEXT_ACCENT: 'var(--text-accent)',
    LINK_COLOR: 'var(--link-color)',
    TAG_BACKGROUND: 'var(--tag-background)',
    DEFAULT_LINK_BORDER: 'rgba(255, 255, 255, 0.204)'
};
